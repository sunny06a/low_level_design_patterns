/*
The iterator Pattern allow traversak of elements of an aggregate without exposing the underlying implementation 
*/

we can also create a menu iterator to iterate different types of menu rather thn repeating code in printmenu() of client/waitress, kind of iterator of iterator (2D)